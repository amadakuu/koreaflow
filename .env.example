# Server-side secrets. Do NOT prefix these with VITE_.
GEMINI_API_KEY=""
GEMINI_MODEL="gemini-2.5-flash"
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
SESSION_SECRET="generate-a-long-random-secret"

# Optional local development URL. Production OAuth redirect uses the request origin.
APP_URL="http://localhost:3000"

# Cloudflare Pages/Functions supplies the D1 binding named DB from wrangler configuration.
